{
	"devDependencies": {
		"husky": "^8.0.3",
		"prettier": "^2.8.7"
	},
	"scripts": {
		"prepare": "husky install",
		"format": "prettier ./**/*.{js,jsx,css,test.js,json} --write",
		"start-fe": "cd front-end && npm run start",
		"start-be": "cd back-end && npm run start"
	}
}
